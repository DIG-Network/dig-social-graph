name: Publish to crates.io

# Tag-driven release: pushing a version tag `vX.Y.Z` runs the build/package checks, publishes to
# crates.io, and cuts a GitHub Release. A normal push to `main` runs the gates only (see ci.yml) — it
# does NOT publish. Mirrors the sibling DIG crates so the same org secrets apply. dig-social-graph
# depends only on already-published crates.io deps (dig-identity, dig-message), so its publish rides
# after those (release-first satisfied).

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., v0.3.1)'
        required: true
        type: string

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Verify the package builds
        run: cargo build --release

      - name: Verify the package can be packaged
        run: cargo package --locked

      - name: Check CARGO_REGISTRY_TOKEN is set
        run: |
          if [ -z "${{ secrets.CARGO_REGISTRY_TOKEN }}" ]; then
            echo "CARGO_REGISTRY_TOKEN secret is not set in repository settings"
            exit 1
          fi

      - name: Publish to crates.io
        run: cargo publish --locked
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: publish
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version from tag
        id: extract_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "dig-social-graph v${{ steps.extract_version.outputs.VERSION }}"
          body: |
            ## dig-social-graph v${{ steps.extract_version.outputs.VERSION }}

            Canonical DIG social-graph core: the networkless, offer-first, symmetric, synchronous
            peer-connection state machine + wire types + four injected seams (Transport,
            EnvelopeSealer, StoreSubscriber, Persistence) that let dig-app maintain its social
            connections. Pure logic — no chain, no network, no crypto, no disk of its own.

            ### Installation
            ```toml
            [dependencies]
            dig-social-graph = "${{ steps.extract_version.outputs.VERSION }}"
            ```
          draft: false
          prerelease: false
